namespace SleepyChat;

internal static class IntegrationConfig
{
    // These are application/operator settings, never end-user settings.
    // Production builds may populate these constants, or use the matching
    // environment variables at launch time.
    public const string KickAuthUrl = "";
    public const string KickRelayUrl = "";
}
